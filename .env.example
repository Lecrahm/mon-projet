# Optionnel. Sans clé, l’app utilise un reformulateur français déterministe (mode démo).
OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini

# Utilisé seulement si OPENAI_API_KEY est absent.
ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL=claude-3-5-haiku-latest
