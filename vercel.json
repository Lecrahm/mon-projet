{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "npm install --prefix candidatures-tracker",
  "buildCommand": "npm run build --prefix candidatures-tracker",
  "outputDirectory": "candidatures-tracker/dist",
  "rewrites": [{ "source": "/((?!assets/).*)", "destination": "/index.html" }]
}
